repos:
  - repo: local
    hooks:
      - id: pytest
        name: Run pytest
        entry: pytest  # Adjust this line if you're using a different command to run tests
        language: system
        pass_filenames: false